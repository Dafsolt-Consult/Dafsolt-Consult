version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: dafsolt-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: dafsolt
      POSTGRES_PASSWORD: dafsolt
      POSTGRES_DB: dafsolt_school
    ports:
      - "5432:5432"
    volumes:
      - dafsolt_pgdata:/var/lib/postgresql/data

volumes:
  dafsolt_pgdata:
