{
  "name": "dafsolt-school-play-deploy",
  "private": true,
  "version": "1.0.0",
  "description": "Uploads a signed Android App Bundle to the Play Console internal testing track via the Play Developer API. Not part of the product — kept separate from client/server so it never ships in an app build.",
  "type": "module",
  "scripts": {
    "publish": "tsx src/publish.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "@types/node": "^22.7.5"
  }
}
